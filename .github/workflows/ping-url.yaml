name: Keep Render service alive

on:
  schedule:
    - cron: "*/10 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: curl -sSf "https://YOUR_RENDER_SERVICE.onrender.com/health" -o /dev/null
